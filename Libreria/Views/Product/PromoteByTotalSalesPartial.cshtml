@model  IEnumerable<ProductViewModel>

@foreach (var item in Model)
{

    @item.Name


@*<li class="col-lg-12">@Html.ActionLink(item.Name,"ProductDetail","Product",new {id=item.Id},null)</li>*@
   
}

@model List<CategoryViewModel>




@foreach (var item in Model)
{

    @Html.ActionLink(@item.Name, "ProductCategory", "Product", new { CategoryId = @item.CategoryId,CategoryName=item.Name }, null)

    
}
